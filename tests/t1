// surenka inputus
// paleisti dar karta
def int double(int i){
        return i*2;
    }

def int[] makeArray(int size){
    int[size] array;
    return array;
}

def int[] fibb(int[] array,int ammount){
    array[0] = 1;
    array[1] = 1;
    for(int i=2; i<ammount; i+=1;){
        int val = 0;
        int j = val;
        array[i] = array[i-1] + array[i-2] + val;
    }
    return array;
}
def int fib(int f1,int f2,int max){
    print(f1);
    if (max==0){
        return f1;
    }
    return fib(f2,f1+f2,max-1);
}

def int mod(int number, int mod){
    if(number<0){
        return mod(number + mod, mod);
    }elif(number>=mod){
        return mod(number - mod, mod);
    }else{
        return number;
    }
}


int a;
int i;
def string concatInt(int a, int b) {
    return toStr(a) + " " + toStr(b);
}
def string concatStr(string a1, string b) {
    return toStr(i) + a1 + " " + b;
    return 90;
}
/*
int number;
int mod;
print("Write number:");
number = toInt(input());
print("Write mod:");
mod = toInt(input());
print("Mod=" + toStr(mod(number,mod)));
*/


int size = 20;
int[size] array;
int o = array[size-1];

array = fibb(array,size);
for(int i=0; i<size; i+=1;){
   // print(array[i]);
}
